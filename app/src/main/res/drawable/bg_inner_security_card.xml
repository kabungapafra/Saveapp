<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- BROAD SOFT SHADOW (Outer spread) -->
    <item
        android:bottom="0dp"
        android:left="12dp"
        android:right="0dp"
        android:top="12dp">
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <solid android:color="#21A3B1C6" /> <!-- 13% alpha deep spread -->
        </shape>
    </item>

    <!-- CORE DARK SHADOW -->
    <item
        android:bottom="0dp"
        android:left="8dp"
        android:right="0dp"
        android:top="8dp">
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <solid android:color="#59A3B1C6" /> <!-- 35% alpha main shadow -->
        </shape>
    </item>

    <!-- CORE LIGHT SHADOW -->
    <item
        android:bottom="8dp"
        android:left="0dp"
        android:right="8dp"
        android:top="0dp">
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <solid android:color="#B3FFFFFF" /> <!-- 70% alpha main light -->
        </shape>
    </item>

    <!-- MAIN CARD BACKGROUND -->
    <item
        android:bottom="6dp"
        android:left="6dp"
        android:right="6dp"
        android:top="6dp">
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <solid android:color="#F0F2F6" />
        </shape>
    </item>
</layer-list>
