<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Dark Shadow (Bottom Right) -->
    <item
        android:bottom="0dp"
        android:left="8dp"
        android:right="0dp"
        android:top="8dp">
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <solid android:color="#99A3B1C6" /> <!-- Exactly 60% alpha -->
        </shape>
    </item>

    <!-- Light Shadow (Top Left) -->
    <item
        android:bottom="8dp"
        android:left="0dp"
        android:right="8dp"
        android:top="0dp">
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <solid android:color="#E6FFFFFF" /> <!-- Exactly 90% alpha -->
        </shape>
    </item>

    <!-- Main Background Color -->
    <item
        android:bottom="6dp"
        android:left="6dp"
        android:right="6dp"
        android:top="6dp">
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <solid android:color="@color/neumorphic_card_bg" />
        </shape>
    </item>
</layer-list>
