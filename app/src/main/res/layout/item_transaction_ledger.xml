<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingVertical="10dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <!-- Member Name + Date Ref -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvTxMemberName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/manrope_bold"
                android:text="Sunrise Cooperative"
                android:textColor="@color/v_text_dark"
                android:textSize="13sp" />

            <TextView
                android:id="@+id/tvTxDateRef"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/manrope_medium"
                android:text="Oct 24 • REF# TXN1"
                android:textColor="@color/v_text_muted"
                android:textSize="11sp" />
        </LinearLayout>

        <!-- Type Badge -->
        <TextView
            android:id="@+id/tvTxTypeBadge"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/bg_badge_success"
            android:backgroundTint="#DCFCE7"
            android:fontFamily="@font/manrope_bold"
            android:gravity="center"
            android:paddingHorizontal="10dp"
            android:paddingVertical="5dp"
            android:text="Contribution"
            android:textColor="#16A34A"
            android:textSize="10sp" />

        <!-- Amount -->
        <TextView
            android:id="@+id/tvTxAmount"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="@font/manrope_bold"
            android:gravity="end"
            android:text="+1,200,000"
            android:textColor="#10B981"
            android:textSize="13sp" />
    </LinearLayout>

    <!-- Thin bottom divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="10dp"
        android:background="@color/v_page_bg" />
</LinearLayout>
