<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Save" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:windowBackground">@color/dashboard_bg</item>
        <item name="android:statusBarColor">@color/dashboard_bg</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:colorBackground">@color/background_primary</item>
    </style>

    <style name="Theme.Save.Dashboard" parent="Base.Theme.Save">
        <item name="android:windowBackground">@color/dashboard_bg</item>
        <item name="android:statusBarColor">@color/dashboard_bg</item>
    </style>
</resources>